mixin footer(small)
  footer.page-footer(class=colorTwo, style='padding-top: 0;')
    style.
      body { display: flex; min-height: 100vh; flex-direction: column; }
      main { flex: 1; }
    if !small
      +footer-info
    +footer-copyright
